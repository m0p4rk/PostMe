<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.postme.mapper.UserMapper">
	<!-- 결과 매핑 정의 -->
	<resultMap id="userResultMap"
		type="com.spring.postme.model.User">
		<!-- 필드 매핑 -->
	</resultMap>

	<!-- 사용자 이름으로 사용자 조회 -->
	<select id="getUserByUsername" parameterType="string"
		resultMap="userResultMap">
		SELECT * FROM users WHERE username = #{username}
	</select>

	<insert id="insertUser"
		parameterType="com.spring.postme.model.User">
		INSERT INTO users (username, password, name, email, isAdmin, createdAt)
		VALUES (#{username}, #{password}, #{name}, #{email}, #{isAdmin},
		#{createdAt})
	</insert>

	<!-- 기타 쿼리들... -->
</mapper>
